# PyInstaller / AppImage build outputs
/build/
/dist/
/.build_venv/
*.spec.bak
__pycache__/
*.pyc

# Generated at build time by packaging/stamp_build.py
build_info.json

# Secrets — never commit
env.local
.env
.env.local
*.token

# Security gate generated reports (baseline + config are tracked)
security/reports/

# Private internal documentation — not for the public repo
/docs/
